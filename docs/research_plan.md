# План исследований и алгоритм экспериментов

## Введение

Данный документ определяет структурированный подход к проведению экспериментов для магистерской работы. План разделён на чёткие итерации с конкретными задачами, метриками успеха и временными рамками.

---

## 📅 Общая временная структура

### Фаза 1: Подготовка и базовые эксперименты (2-3 недели)
### Фаза 2: Разработка системы мониторинга (2-3 недели)
### Фаза 3: Комплексные эксперименты и валидация (2-3 недели)
### Фаза 4: Анализ результатов и подготовка к защите (1-2 недели)

**Итого: 8-11 недель** (с учетом запаса времени)

---

## 🔬 Итерация 1: Базовые эксперименты с данными и моделями

### Цель итерации
Подтвердить работоспособность базового пайплайна и получить первые метрики для сравнения.

### Задачи

#### 1.1 Подготовка данных (3-5 дней)
- [ ] Загрузка Adult Income dataset (UCI)
- [ ] Анализ структуры данных (EDA)
- [ ] Предобработка (нормализация, кодирование категориальных признаков)
- [ ] Разделение на train/test (80/20)
- [ ] Генерация синтетических данных для тестов дрейфа
- [ ] Документирование характеристик датасетов

**Выходные артефакты:**
- Очищенные датасеты в `data/processed/`
- Jupyter notebook с EDA (`notebooks/01_data_exploration.ipynb`)
- Таблица со статистикой датасетов

#### 1.2 Обучение базовых моделей (5-7 дней)
- [ ] Обучение моделей на Adult Income:
  - Logistic Regression
  - Random Forest
  - XGBoost
  - LightGBM
- [ ] Настройка гиперпараметров (GridSearchCV для 2-3 моделей)
- [ ] Сохранение всех обученных моделей

**Выходные артефакты:**
- Обученные модели в `models/`
- Таблица сравнения моделей (accuracy, F1, ROC-AUC)
- Jupyter notebook (`notebooks/02_model_training.ipynb`)

#### 1.3 Базовая оценка производительности (3-4 дня)
- [ ] Вычисление метрик для всех моделей:
  - Accuracy, Precision, Recall, F1
  - ROC-AUC, PR-AUC
  - Confusion Matrix
- [ ] Измерение latency (средняя, p95, p99)
- [ ] Создание визуализаций (ROC curves, confusion matrices)

**Критерии успеха:**
- Все модели показывают accuracy > 0.75
- ROC-AUC > 0.80 для лучшей модели
- Latency < 100ms для большинства моделей (CPU)

**Выходные артефакты:**
- Отчет с метриками (`results/iteration1_performance.csv`)
- Графики производительности
- Выбранная базовая модель для дальнейших экспериментов

---

## 🔬 Итерация 2: Внедрение и валидация методов детекции дрейфа

### Цель итерации
Реализовать методы детекции дрейфа и проверить их эффективность на контролируемых данных.

### Задачи

#### 2.1 Реализация метрик дрейфа (3-4 дня)
- [ ] Реализация KS-теста для числовых признаков
- [ ] Реализация Chi-Square теста для категориальных признаков
- [ ] Реализация PSI (Population Stability Index)
- [ ] Реализация Wasserstein Distance
- [ ] Unit-тесты для всех методов

**Критерии успеха:**
- Все методы корректно работают на тестовых данных
- Тесты покрывают > 80% кода

#### 2.2 Генерация дрейфа для экспериментов (2-3 дня)
- [ ] Создание функций генерации дрейфа:
  - Низкая интенсивность (0.1)
  - Средняя интенсивность (0.3)
  - Высокая интенсивность (0.5)
- [ ] Генерация дрейфа для разных типов признаков (числовые, категориальные)
- [ ] Сохранение сгенерированных данных

**Выходные артефакты:**
- Функция `create_drift_data()` в `src/data/preprocess.py`
- Тестовые датасеты с дрейфом в `data/synthetic/drift_*`

#### 2.3 Эксперименты по детекции дрейфа (5-7 дней)
- [ ] Эксперимент 2.3.1: Сравнение методов на разных уровнях дрейфа
  - Для каждой интенсивности (0.1, 0.3, 0.5):
    - Применить все методы детекции
    - Измерить: sensitivity, specificity, время выполнения
    - Создать таблицу результатов
  
- [ ] Эксперимент 2.3.2: Влияние размера выборки
  - Протестировать на выборках: 100, 500, 1000, 5000
  - Записать метрики для каждого размера
  
- [ ] Эксперимент 2.3.3: Ложные срабатывания (false positives)
  - Тест на данных без дрейфа (той же выборке)
  - Подсчет false positive rate для каждого метода

**Протокол эксперимента 2.3.1:**

```python
# Псевдокод для четкого алгоритма

for intensity in [0.1, 0.3, 0.5]:
    # Генерируем дрейф
    X_drifted = create_drift_data(X_test, drift_intensity=intensity)
    
    # Инициализируем детектор
    detector = DriftDetector(X_test, threshold=0.05)
    
    # Тестируем каждый метод
    for method in ['ks', 'chi2', 'psi', 'wasserstein']:
        start_time = time.time()
        results = detector.detect_drift(X_drifted, methods=[method])
        execution_time = time.time() - start_time
        
        # Сохраняем результаты
        save_results({
            'intensity': intensity,
            'method': method,
            'drift_detected': results['summary']['drift_detected'],
            'execution_time': execution_time,
            'features_with_drift': count_drifted_features(results)
        })
```

**Критерии успеха:**
- При интенсивности 0.3+ все методы детектируют дрейф
- PSI показывает лучшие результаты для сильного дрейфа
- False positive rate < 5% для всех методов

**Выходные артефакты:**
- Таблица результатов: `results/iteration2_drift_detection.csv`
- Графики: sensitivity vs intensity для каждого метода
- Рекомендации по выбору метода

---

## 🔬 Итерация 3: Мониторинг производительности и деградации

### Цель итерации
Реализовать систему мониторинга производительности и проверить её способность обнаруживать деградацию.

### Задачи

#### 3.1 Реализация мониторинга производительности (3-4 дня)
- [ ] Реализация класса `PerformanceMonitor`
- [ ] Метрики: accuracy, precision, recall, F1, ROC-AUC
- [ ] Измерение latency и throughput
- [ ] Детекция деградации (сравнение с baseline)

#### 3.2 Эксперименты по симуляции деградации (5-7 дней)
- [ ] Эксперимент 3.2.1: Симуляция постепенной деградации
  - Создать последовательность датасетов с постепенным ухудшением качества данных
  - Применить модель на каждом датасете
  - Отследить изменения метрик во времени
  - Проверить способность детекции деградации на разных порогах (5%, 10%, 15%)

- [ ] Эксперимент 3.2.2: Сравнение latency разных моделей
  - Измерить latency для всех моделей на одинаковых данных
  - Сравнить XGBoost vs Random Forest vs Logistic Regression
  - Проанализировать trade-off accuracy vs latency

**Протокол эксперимента 3.2.1:**

```python
# Генерируем серию данных с постепенным дрейфом
degradation_steps = [0.0, 0.05, 0.1, 0.15, 0.2, 0.3, 0.4, 0.5]
results_timeline = []

for step, drift in enumerate(degradation_steps):
    # Генерируем данные с текущим уровнем дрейфа
    X_current = create_drift_data(X_test, drift_intensity=drift)
    
    # Получаем предсказания (симулируем, что модель не обновляется)
    y_pred = model.predict(X_current)
    
    # Вычисляем метрики
    accuracy = accuracy_score(y_test, y_pred)
    
    # Мониторинг
    monitor = PerformanceMonitor(model, baseline_metrics=baseline)
    degradation = monitor.detect_degradation({
        'accuracy': accuracy,
        'f1': f1_score(y_test, y_pred)
    })
    
    # Сохраняем
    results_timeline.append({
        'step': step,
        'drift_intensity': drift,
        'accuracy': accuracy,
        'degradation_detected': degradation['degradation_detected'],
        'threshold': degradation['threshold']
    })
    
# Визуализация timeline
plot_degradation_timeline(results_timeline)
```

**Критерии успеха:**
- Деградация детектируется при падении accuracy на 5%+
- Latency всех моделей < 200ms на CPU
- Система корректно отслеживает изменения во времени

**Выходные артефакты:**
- График деградации во времени
- Таблица сравнения latency: `results/iteration3_latency_comparison.csv`
- Рекомендации по порогам для алертов

---

## 🔬 Итерация 4: Анализ справедливости

### Цель итерации
Реализовать метрики справедливости и провести анализ на реальных и синтетических данных.

### Задачи

#### 4.1 Реализация метрик справедливости (3-4 дня)
- [ ] Demographic Parity Difference
- [ ] Equal Opportunity Difference
- [ ] Equalized Odds Difference
- [ ] Disparate Impact Ratio
- [ ] Групповые метрики производительности

#### 4.2 Эксперименты по анализу справедливости (5-7 дней)
- [ ] Эксперимент 4.2.1: Анализ на Adult Income dataset
  - Выбрать чувствительные признаки (gender, race если доступно)
  - Вычислить все метрики справедливости
  - Определить группы с наибольшими различиями
  
- [ ] Эксперимент 4.2.2: Сравнение справедливости разных моделей
  - Обучить несколько моделей на одинаковых данных
  - Сравнить fairness метрики между моделями
  - Выявить модель с наилучшим балансом accuracy/fairness

- [ ] Эксперимент 4.2.3: Синтетические данные с контролируемым bias
  - Сгенерировать данные с явным bias между группами
  - Обучить модель и измерить fairness метрики
  - Проверить способность метрик обнаружить bias

**Протокол эксперимента 4.2.1:**

```python
# Загружаем данные
X_train, X_test, y_train, y_test = load_adult_data()

# Определяем чувствительные признаки
sensitive_features = ['sex', 'race']  # если доступно

# Обучаем модель
model = train_model(X_train, y_train)
y_pred = model.predict(X_test)

# Анализ справедливости
monitor = FairnessMonitor(sensitive_features, threshold=0.1)
report = monitor.comprehensive_fairness_report(X_test, y_test, y_pred)

# Сохраняем результаты
for feature in sensitive_features:
    metrics = report['fairness_metrics'][feature]
    group_perf = report['group_performance'][feature]
    
    save_fairness_results({
        'feature': feature,
        'demographic_parity_diff': metrics['demographic_parity_difference'],
        'equal_opportunity_diff': metrics['equal_opportunity_difference'],
        'disparate_impact_ratio': metrics['disparate_impact_ratio'],
        'groups': group_perf
    })
```

**Критерии успеха:**
- Все метрики корректно вычисляются
- Обнаруживаются различия между группами (> 0.1 threshold)
- Результаты визуализированы и интерпретируемы

**Выходные артефакты:**
- Отчет по справедливости: `results/iteration4_fairness_report.csv`
- Визуализации групповых различий
- Таблица сравнения моделей по fairness

---

## 🔬 Итерация 5: Интеграция и end-to-end тестирование

### Цель итерации
Объединить все компоненты в единую систему и провести комплексное тестирование.

### Задачи

#### 5.1 Развертывание API и Dashboard (3-5 дней)
- [ ] Настройка FastAPI endpoints
- [ ] Интеграция всех модулей мониторинга
- [ ] Создание Streamlit dashboard
- [ ] Тестирование всех функций через API

#### 5.2 End-to-end эксперименты (5-7 дней)
- [ ] Эксперимент 5.2.1: Полный цикл мониторинга
  - Обучить модель
  - Развернуть через API
  - Симулировать поток данных с дрейфом
  - Проверить работу всех компонентов мониторинга одновременно:
    - Performance monitoring
    - Drift detection
    - Fairness analysis
  
- [ ] Эксперимент 5.2.2: Нагрузочное тестирование
  - Измерить throughput API (requests/second)
  - Протестировать latency при различной нагрузке
  - Проверить стабильность системы

**Критерии успеха:**
- Все компоненты работают вместе без ошибок
- API обрабатывает > 100 requests/second
- Dashboard корректно отображает все метрики

**Выходные артефакты:**
- Развернутая система (API + Dashboard)
- Скриншоты dashboard
- Отчет по нагрузочному тестированию

---

## 🔬 Итерация 6: Финальная валидация и бенчмаркинг

### Цель итерации
Собрать все результаты, сравнить с существующими решениями, подготовить финальный отчет.

### Задачи

#### 6.1 Сравнение с существующими решениями (3-5 дней)
- [ ] Установить и протестировать Evidently AI (или аналог)
- [ ] Сравнить метрики детекции дрейфа
- [ ] Сравнить производительность (latency, memory usage)
- [ ] Таблица сравнения: наша система vs существующие

#### 6.2 Финальные эксперименты (3-5 дней)
- [ ] Повторить ключевые эксперименты для стабильности результатов
- [ ] Собрать все результаты в единые таблицы
- [ ] Создать итоговые визуализации
- [ ] Подготовить выводы и рекомендации

#### 6.3 Подготовка отчета (5-7 дней)
- [ ] Структурирование всех результатов
- [ ] Написание интерпретаций экспериментов
- [ ] Подготовка презентации с результатами
- [ ] Документирование ограничений и будущих работ

**Выходные артефакты:**
- Финальный отчет: `results/final_report.md`
- Таблица сравнения систем
- Финальная презентация

---

## 📊 Шаблон для протокола эксперимента

Для каждого эксперимента создавайте файл в формате:

```markdown
# Эксперимент [номер]: [название]

## Дата проведения: [дата]
## Итерация: [номер]
## Исследователь: [ваше имя]

## Цель
[Что хотите доказать/проверить]

## Гипотеза
[Какую гипотезу проверяете]

## Методология
[Подробное описание шагов]

## Данные
- Датасет: [название]
- Размер выборки: [число]
- Параметры: [список]

## Результаты
- Метрика 1: [значение]
- Метрика 2: [значение]
- Графики: [ссылки]

## Выводы
[Что узнали, подтвердилась ли гипотеза]

## Ограничения
[Что могло повлиять на результаты]
```

---

## ⏱️ Временные рамки и контрольные точки

### Чекпоинт 1 (через 2 недели)
- [ ] Завершена итерация 1
- [ ] Обучены базовые модели
- [ ] Получены первые метрики

### Чекпоинт 2 (через 4-5 недель)
- [ ] Завершены итерации 2-3
- [ ] Детекция дрейфа работает и протестирована
- [ ] Мониторинг производительности реализован

### Чекпоинт 3 (через 7-8 недель)
- [ ] Завершены итерации 4-5
- [ ] Fairness анализ проведен
- [ ] Система интегрирована и работает end-to-end

### Чекпоинт 4 (через 9-10 недель)
- [ ] Все эксперименты завершены
- [ ] Результаты собраны и проанализированы
- [ ] Отчет подготовлен

---

## 📈 Метрики успеха проекта

### Технические метрики
- ✅ Все модули реализованы и протестированы
- ✅ Система работает end-to-end без критических ошибок
- ✅ API latency < 200ms для одной предсказания
- ✅ Детекция дрейфа работает с accuracy > 90% для сильного дрейфа

### Метрики исследования
- ✅ Проведено не менее 10 структурированных экспериментов
- ✅ Все эксперименты документированы и воспроизводимы
- ✅ Результаты сравнены с существующими решениями
- ✅ Выявлены преимущества и ограничения подхода

### Метрики для диссертации
- ✅ Достаточно данных для всех глав диссертации
- ✅ Результаты визуализированы и интерпретированы
- ✅ Сделаны выводы и рекомендации
- ✅ Подготовлена презентация для защиты

---

## 🚨 Управление рисками

### Риск: Нехватка времени
**Митигация:** 
- Фокус на ключевых экспериментах (отмечены ⭐)
- Параллельная работа (например, обучение моделей пока идет загрузка данных)
- Готовые шаблоны для экспериментов

### Риск: Проблемы с данными
**Митигация:**
- Использование синтетических данных как запасной вариант
- Начало работы с данными как можно раньше

### Риск: Технические сложности
**Митигация:**
- Использование готовых библиотек где возможно
- Фокус на функциональности, а не на оптимизации
- Документирование известных ограничений

---

## 📝 Рекомендации по ведению журнала экспериментов

1. **Используйте Jupyter notebooks** для всех экспериментов
2. **Сохраняйте все результаты** в `results/` с понятными именами
3. **Коммитьте код** после каждой итерации
4. **Создавайте скриншоты** dashboard и визуализаций
5. **Ведите changelog** важных изменений

---

## 🎯 Ключевые эксперименты (приоритетные)

Если время ограничено, фокус на этих экспериментах:

1. ⭐ **Эксперимент 2.3.1**: Сравнение методов детекции дрейфа
2. ⭐ **Эксперимент 3.2.1**: Симуляция деградации и детекция
3. ⭐ **Эксперимент 4.2.1**: Анализ справедливости на Adult Income
4. ⭐ **Эксперимент 5.2.1**: End-to-end тестирование системы
5. ⭐ **Эксперимент 6.1**: Сравнение с существующими решениями

Эти 5 экспериментов дадут достаточно материала для диссертации!

