# Data drift detection

Minimal scaffold for Step 1: statistical drift vs a training baseline (PSI, KS, Chi-square).

- Put dataset files in `data/raw/` (see `data/raw/README.txt`).
- Implementation lives in `src/drift_detection/`.

```bash
pip install -r requirements.txt
```
