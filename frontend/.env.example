# Optional: full API base URL (overrides dev proxy default).
# Leave unset in dev to use Vite proxy to http://127.0.0.1:8000
# VITE_API_BASE_URL=http://127.0.0.1:8000

# Optional: where Vite proxies /api (default 127.0.0.1:8000). Used by vite.config.ts only.
# VITE_PROXY_API=http://127.0.0.1:8000
