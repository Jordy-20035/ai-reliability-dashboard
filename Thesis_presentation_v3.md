# Thesis presentation v3 — slide-by-slide outline
# (Russian, для защиты диплома. ~20 слайдов.)
# Базируется на v2 + расширения из заполненного диплома
# (главы 2.1, 4.1, 4.2-4.5, таблицы 1-5).

---

## Slide 1 — Cover (Титульный слайд)

**Header (small, top-center):**
Министерство науки и высшего образования Российской Федерации
Челябинский государственный университет
Факультет [название]   ·   Кафедра [название]

**Title (large, centered):**
Разработка программного комплекса для детектирования дрифта данных
и адаптивного переобучения моделей машинного обучения

**Sub-title (small, centered):**
Выпускная квалификационная работа магистранта
направление 09.04.04 «Программная инженерия»

**Footer (two columns, small):**
- *Студент:* Н. М. Джордана, группа МПрИ-201
- *Научный руководитель:* [И.О. Фамилия], [должность, уч. степень]

**Year (bottom-center):** Челябинск, 2026 г.

---

## Slide 2 — Актуальность

**Header:** Почему это важно

**4-card layout (numbered 1–4):**

1. **Data drift** — Сдвиг распределений входных данных относительно
   распределений при обучении. Не виден в коде, но «съедает» качество.

2. **Скрытая деградация** — Модель работает, но качество падает —
   ошибки не выбрасываются, метрики молча ухудшаются.

3. **Недостаточность одних детекторов** — Нужен весь контур: аудит,
   переобучение, версионирование, продвижение в production.

4. **Фрагментация инструментов** — Evidently / NannyML / MLflow / Airflow
   решают каждый свою часть; их связка — отдельная инженерная задача.

**Bottom (highlighted):** Цель — единая платформа вместо набора
разрозненных MLOps-инструментов.

---

## Slide 3 — Цель и задачи работы

**Header:** Цель работы

> Разработка программного комплекса для детектирования дрифта данных
> и адаптивного переобучения моделей машинного обучения.

**Sub-header:** Задачи

1. Выполнить анализ предметной области.
2. Спроектировать архитектуру интегрированной MLOps-платформы.
3. Реализовать механизмы детектирования дрифта, оркестрации,
   автоматического переобучения и управления жизненным циклом моделей.
4. Разработать REST API и веб-панель мониторинга.
5. Провести экспериментальную валидацию на реальных и синтетических
   наборах данных.
6. Сформулировать выводы и направления дальнейшего развития.

---

## Slide 4 — Анализ аналогов

**Header:** Сравнение с существующими инструментами

| Инструмент       | Детект. дрифта | Реестр моделей | Оркестрация | Автопереобучение | Open-source |
|------------------|:--------------:|:--------------:|:-----------:|:----------------:|:-----------:|
| Evidently AI     | ●              | ○              | ○           | ○                | ●           |
| NannyML          | ●              | ○              | ○           | ○                | ●           |
| MLflow           | ◐              | ●              | ○           | ○                | ●           |
| Weights & Biases | ◐              | ●              | ○           | ○                | ◐           |
| Apache Airflow   | ○              | ○              | ●           | ◐                | ●           |
| Fiddler AI       | ●              | ◐              | ○           | ○                | ○           |
| **Наша платформа** | **●**        | **●**          | **●**       | **●**            | **●**       |

**Легенда:** ● полностью; ◐ частично; ○ нет.

**Вывод:** ни одно из существующих решений не покрывает полный цикл
**drift → policy → retrain → lifecycle → inference** в одной системе.

---

## Slide 5 — Статистические методы детектирования дрифта

**3-column layout:**

**PSI (Population Stability Index)**
- PSI = Σ (Qᵢ − Pᵢ) · ln(Qᵢ / Pᵢ)
- Бины: 10 квантильных интервалов
- Бэнды: < 0.10 stable; 0.10–0.25 moderate; ≥ 0.25 high
- Интерпретируем, эффективен для финансовых задач

**KS (Колмогоров–Смирнов)**
- D = supₓ |Fₙ(x) − Gₘ(x)|
- α = 0.05
- Применим к численным признакам
- Нон-параметрический, distribution-free

**χ² (Хи-квадрат)**
- χ² = Σ (Oᵢ − Eᵢ)² / Eᵢ
- α = 0.05
- Применим к категориальным признакам
- Редкие категории объединяются в `__other__`

**Сводка снизу:** Три метода покрывают численные + категориальные признаки,
с низкой вычислительной сложностью и интерпретируемым выходом.

---

## Slide 6 — Функциональные и нефункциональные требования (НОВЫЙ)

**2-column layout:**

**Функциональные требования (8)**
1. Вычислять per-feature PSI / KS / χ² и агрегатные счётчики дрейфа.
2. Сохранять и переиспользовать **frozen** baseline profile.
3. Оценивать пороговую политику и возвращать (триггер, причины).
4. Запускать цепочку действий: логирование, webhook, переобучение.
5. Переобучать на объединённой выборке reference+current,
   регистрировать новую версию модели, продвигать по правилу.
6. Предоставлять REST API ко всем компонентам.
7. Предоставлять веб-панель мониторинга и управления.
8. Записывать provenance: датасет, baseline, эксперимент, версия модели.

**Нефункциональные требования (6)**
1. Один хост, только open-source зависимости.
2. Backend на Python + FastAPI; frontend на React/MUI.
3. Хранилище: 3 встроенные SQLite БД в одной директории `artifacts/`.
4. Воспроизводимое развёртывание через Docker Compose
   (3 сервиса: api / scheduler / frontend).
5. Расширяемость: добавление новой метрики / действия / модели —
   изменения только в соответствующем модуле.
6. Все рандомизированные операции — детерминированы при фиксированном
   `random_state = 42`.

---

## Slide 7 — Архитектура системы

**Header:** Архитектура: 5 модулей + 2 интерфейса

```
┌───────────────────────────────────────────────────────────┐
│  REST API (FastAPI)             SPA Dashboard (React/MUI) │
└─────────────────────────┬─────────────────────────────────┘
                          │
       ┌──────────────────┼──────────────────┐
       │                  │                  │
┌──────▼──────┐  ┌────────▼────────┐  ┌──────▼──────┐
│   drift_    │  │  orchestration  │  │  retraining │
│  detection  │  │  (Engine +      │  │  (pipeline +│
│  (PSI/KS/χ²)│  │   Policy +      │  │   registry +│
│             │  │   Actions +     │  │   promotion)│
│             │  │   RunStore)     │  │             │
└─────────────┘  └────────┬────────┘  └──────┬──────┘
                          │                  │
                 ┌────────▼──────────────────▼────────┐
                 │  lifecycle      data_management    │
                 │  (stages,       (dataset versions, │
                 │   registry,     baselines,         │
                 │   promotion)    provenance)        │
                 └────────────────────────────────────┘
                          │
                 ┌────────▼────────┐
                 │  SQLite × 3     │
                 │  orchestration  │
                 │  lifecycle      │
                 │  data_mgmt      │
                 └─────────────────┘
```

**Подпись:** Один запуск оркестрации =
drift report → policy → actions → optional retrain →
lifecycle update → provenance.

---

## Slide 8 — Политика и пороги

**Header:** DriftThresholdPolicy: пороги срабатывания

| Счётчик                       | Порог | Логика                                  |
|-------------------------------|:-----:|-----------------------------------------|
| n_features_high_psi           |  > 0  | Любая «high»-PSI фича запускает триггер |
| n_numeric_ks_significant      |  > 2  | Допускаем ~2 KS-FP при α=0.05            |
| n_categorical_chi2_significant|  > 3  | Допускаем ~3 χ²-FP при α=0.05            |

**Action chain** (выполняется при срабатывании):

1. **LogAction** — структурированное логирование.
2. **WebhookAlertAction** — опциональный POST на внешний webhook.
3. **RetrainPipelineAction** — запуск пайплайна переобучения.

**Каждый запуск** (триггерный или нет) сохраняется в SQLite
(`artifacts/orchestration.db`) с timestamp, scenario, причинами и
JSON-сводкой отчёта о дрейфе → полная аудиторская история.

---

## Slide 9 — Жизненный цикл моделей

**Header:** Lifecycle: development → staging → production → archived

```
   ┌─────────────┐
   │ development │  (новая обученная модель по умолчанию)
   └──────┬──────┘
          │ promote_stage
   ┌──────▼──────┐
   │   staging   │  (валидация на свежих данных)
   └──────┬──────┘
          │ promote_stage (production)
   ┌──────▼──────┐       ┌─────────────┐
   │ production  │──────►│  archived   │  (предыдущая prod-модель)
   └─────────────┘ auto  └─────────────┘
```

**Правило продвижения (RetrainPipelineAction):**
Новая модель становится production ⇔ её **macro-F1 ≥ macro-F1 чемпиона**.

Иначе модель регистрируется как `development` с пометкой
`f1_macro_below_champion`, и production-указатель **не меняется**.

**Гарантия безопасности:** ни при каких условиях production-модель не
ухудшается по выбранной метрике.

---

## Slide 10 — Технологический стек

**4-card layout:**

**Backend**
- Python 3.11
- FastAPI (REST API + OpenAPI docs)
- scikit-learn (HistGradientBoostingClassifier)
- scipy / numpy / pandas
- SQLite (3 БД: orchestration / lifecycle / data_management)

**Frontend**
- TypeScript + React (Vite)
- Material-UI (MUI)
- recharts (графики) + @mui/x-data-grid
- axios (HTTP client)

**Развёртывание**
- Docker Compose (3 сервиса: api / scheduler / frontend)
- Один общий volume `./artifacts` (БД + артефакты моделей)

**Хранение артефактов**
- `artifacts/models/model_vN.joblib`
- `registry.json`, `champion.json`
- `artifacts/{orchestration,lifecycle,data_management}.db`

---

## Slide 11 — REST API и панель мониторинга (НОВЫЙ)

**Header:** Единая поверхность управления

**REST API (FastAPI) — 13 endpoints:**

| Чтение                                | Запись                              |
|---------------------------------------|--------------------------------------|
| `GET /api/overview`                   | `POST /api/orchestration/check-once` |
| `GET /api/orchestration/runs`         | `POST /api/retraining/run`           |
| `GET /api/ops/stats`                  | `POST /api/lifecycle/promote`        |
| `GET /api/lifecycle/models`           | `POST /api/inference/predict`        |
| `GET /api/lifecycle/experiments`      |                                      |
| `GET /api/lifecycle/production`       |                                      |
| `GET /api/data/datasets`              |                                      |
| `GET /api/data/baselines`             |                                      |
| `GET /api/data/provenance`            |                                      |

**Дашборд (React SPA) — 5 страниц:**
**Overview** (KPI + график) · **Workflows** (журнал runs) ·
**Models** (реестр + promote/retrain) · **Data** (provenance) ·
**Inference** (playground).

**Принцип:** дашборд использует **только** публичный REST API,
поэтому может быть развёрнут независимо от бекенда.

---

## Slide 12 — Стратегия экспериментальной валидации (НОВЫЙ)

**Header:** 4 оси валидации

1. **Валидация детектирования дрифта** — счётчики на синтетических
   и реальных распределениях, включая «no-drift» как baseline.

2. **Валидация переобучения** — детерминированность при фиксированных
   seeds, целостность реестра, безопасность правила продвижения.

3. **Интеграционные тесты** — end-to-end по всем 6 сценариям:
   drift report → policy → actions → registry → lifecycle → provenance.

4. **Практическая апробация** — наблюдаемые счётчики дрейфа и метрики
   переобученных моделей на Adult и Credit Card Fraud.

**Условия воспроизводимости:**
- `random_state = 42` для всех разбиений и моделей.
- Стратифицированный holdout 0.20 для обучения.
- orchestration ref/cur split = 0.30 (Adult).
- Fraud — 3 равных temporal-окна D1 / D2 / D3 по Time.
- Окружение: Python 3.11, scikit-learn 1.4, FastAPI 0.110, Docker 26.x.

---

## Slide 13 — Наборы данных (НОВЫЙ — таблица 1 из диплома)

**Header:** Adult Census Income · Credit Card Fraud

| Набор / разбивка       | Записи  | Признаки               | Положит. класс | Разбиение                                |
|------------------------|---------|------------------------|---------------|------------------------------------------|
| Adult                  | 48 842  | 14 (6 чис. + 8 кат.)   | ≈ 24%         | train/test = 0.7 / 0.3                   |
| Adult — random_holdout | 48 842  | 14                     | ≈ 24%         | ref ≈ 34 189; cur ≈ 14 653               |
| Adult — age_shift      | 48 842  | 14                     | ≈ 24%         | ref ≈ 34 189; cur = age ≥ 40 (≈ 19 566)  |
| Credit Card Fraud      | 284 807 | 29 (28 V + Amount)     | ≈ 0.17%       | окна по Time: D1 / D2 / D3               |
| Fraud — D1             | ≈ 94 936| 29                     | ≈ 0.17%       | 0 ≤ Time < t₁                            |
| Fraud — D2             | ≈ 94 935| 29                     | ≈ 0.17%       | t₁ ≤ Time < t₂                           |
| Fraud — D3             | ≈ 94 936| 29                     | ≈ 0.17%       | t₂ ≤ Time ≤ tмакс                        |

**Почему два разных датасета:**
Adult — смешанные признаки (численные + категориальные), управляемый
дрейф (`age_shift`). Fraud — высокая несбалансированность (0.17%) и
**естественный темпоральный дрейф**.

---

## Slide 14 — Эксперименты: Adult Census Income

**Header:** random_holdout vs age_shift

| Сценарий       | n_high_psi | n_ks_sig | n_chi2_sig | Триггер |
|----------------|:----------:|:--------:|:----------:|:-------:|
| random_holdout |      0     |   0–2    |    0–1     |   нет   |
| age_shift      |      4     |    5     |     5      |   да    |

**Наблюдения:**

- На **random_holdout** политика не срабатывает: счётчики в пределах
  ожидаемого статистического шума при α=0.05 (≈ 0.4 ожидаемых FP χ²
  на 8 категориальных признаков → 0–1 — модальный случай).

- На **age_shift** все три счётчика превышают пороги → политика
  срабатывает → запускается пайплайн переобучения.

**Вывод:** пороги политики корректно калиброваны — система не реагирует
на статистический шум, но реагирует на реальный сдвиг.

---

## Slide 15 — Эксперименты: Credit Card Fraud (темпоральный дрейф)

**Header:** D1 / D2 / D3 (разбиение по Time)

**Распределение fraud-класса по окнам:**

| Период | Записи  | fraud |
|--------|---------|-------|
| D1     | 94 935  | 217   |
| D2     | 94 936  | 153   |
| D3     | 94 936  | 122   |

**Дрейф между темпоральными окнами (29 численных признаков):**

| Сценарий          | n_high_psi | n_ks_sig | Средний PSI | Триггер |
|-------------------|:----------:|:--------:|:-----------:|:-------:|
| fraud_d1_vs_d2    |      3     |     7    |    0.11     |   да    |
| fraud_d2_vs_d3    |      5     |     9    |    0.16     |   да    |
| fraud_d1_vs_d3    |      8     |    12    |    0.23     |   да    |

**Ключевое наблюдение:** монотонное усиление дрейфа с увеличением
темпорального расстояния — соответствует физической природе данных
(реальные изменения за реальное время).

---

## Slide 16 — Результаты адаптивного переобучения

**Header:** Сравнение моделей: до и после переобучения

| Обучение            | Тест | ROC-AUC | **PR-AUC** | Recall |
|---------------------|:----:|:-------:|:----------:|:------:|
| D1 only             |  D2  |  0.884  |  **0.668** | 0.784  |
| D1 only (baseline)  |  D3  |  0.949  |  **0.609** | 0.820  |
| **D1+D2 (adapted)** |  D3  |  0.978  | **🟢 0.746** | 0.877  |

**Ключевой результат:** переобучение на свежих данных (D2) увеличило
PR-AUC на тестовом периоде D3 с **0.609 → 0.746 (+0.137)**.

**Качество переобученных моделей по сценариям (holdout 20%):**

| Сценарий              | accuracy | macro-F1 | Продвинуто |
|-----------------------|:--------:|:--------:|:----------:|
| random_holdout (Adult)|  0.866   |  0.819   |    да      |
| age_shift (Adult)     |  0.866   |  0.818   |    нет     |
| fraud_retrain_d1_d2   |  0.9994  |  0.917   |    да      |

**Демонстрация безопасности:** age_shift регистрирует новую версию,
но **не продвигает** её — production-указатель сохраняется.

---

## Slide 17 — Демонстрация платформы: единый MLOps-цикл

**Header:** 5 страниц дашборда (один UI на 4 модуля)

**Cards (со скриншотами):**

1. **Overview** — KPI-карточки (Drift score / Workflow Runs / Model
   Versions / Production Row), график сигналов дрейфа, ручной запуск
   проверок.

2. **Workflows** — журнал всех runs, статус (triggered / ok),
   детальные причины срабатывания.

3. **Models** — реестр моделей с этапами, кнопки `Trigger Retraining`
   и `Promote` (продвижение в production).

4. **Data** — версии наборов данных, baseline snapshots и provenance
   (что обучалось на чём и какой baseline использовалось).

5. **Inference** — playground для тестирования: выбор профиля
   (`adult` / `fraud`), JSON-ввод, наглядный результат с вероятностью.

---

## Slide 18 — Валидация системы (НОВЫЙ — итог гл. 4.2-4.4)

**Header:** Подтверждённые свойства реализации

**Детектирование дрейфа**
- На одинаковых reference и current: PSI ≈ 0, p-value ≈ 1 → счётчики 0.
- На случайной подвыборке: счётчики в пределах α·N (стат. шум).
- На сильно сдвинутой выборке (age_shift, D1↔D3): high-PSI > 0 → триггер.

**Автоматическое переобучение**
- Детерминированность: одинаковые входы + seed → одинаковые метрики.
- Целостность реестра: 1 retrain → 1 запись в `registry.json` + 1 эксперимент
  + 1 model_version в `lifecycle.db`.
- **Безопасность продвижения:** production-указатель обновляется ⇔ новая
  метрика ≥ метрика чемпиона.
- Provenance: каждый retrain создаёт `training_provenance` со ссылками
  на dataset_version + baseline_snapshot + experiment + model_version.

**Интеграционные тесты** прошли успешно для всех 6 сценариев
(random_holdout, age_shift, incoming_csv, fraud_d1_vs_d2/d2_vs_d3/d1_vs_d3).

---

## Slide 19 — Вклад, ограничения, дальнейшее развитие

**3-column layout:**

**Вклад работы**
- Интегрированный MLOps-контур: drift → decision → retrain → lifecycle
  → inference в **одной** системе.
- Реализация для двух разных доменов (Adult + Fraud temporal).
- Прослеживаемость: provenance связывает датасет, baseline, эксперимент,
  версию модели.
- Открытая, воспроизводимая платформа (Docker Compose, SQLite, MIT-style).

**Практическая ценность**
- Полная прозрачность и аудит решений модели.
- Управляемое, предсказуемое обновление: правило **«не хуже чемпиона»**.
- Снижение интеграционных издержек: нет связки 4-5 разных тулов.
- Подходит как референс-имплементация и стартовая точка для команд.

**Ограничения и развитие**
- Демо-датасеты и эвристические пороги (требуют калибровки на проде).
- Нет enterprise-аутентификации (RBAC, SSO).
- Single-host развёртывание (Docker Compose, не Kubernetes).

**Дальнейшее развитие:**
- Staging-канареечное развёртывание + human-in-the-loop.
- Обучаемые политики (learnable policies), учитывающие бизнес-издержки.
- Drift detection для не-табличных данных (эмбеддинг-дрейф).
- Интеграция с промышленными системами (Airflow, MLflow) без потери
  бизнес-логики платформы.

---

## Slide 20 — Заключение

**Header:** Что сделано

В рамках работы был разработан **программный комплекс**, обеспечивающий
**сквозной MLOps-цикл**:

✓ Детектирование дрейфа данных (PSI, KS, χ²)
✓ Пороговая политика и контролируемые действия
✓ Автоматизированное переобучение моделей
✓ Управление жизненным циклом и продвижение версий
✓ Прослеживаемость данных и экспериментов
✓ REST API и веб-панель мониторинга
✓ Воспроизводимое развёртывание через Docker Compose
✓ Экспериментальная валидация на 2 наборах данных, 6 сценариях

**Цель работы достигнута.**

---

## Slide 21 — Спасибо

**Centered, large:** Спасибо за внимание!

**Sub-text (optional):**
GitHub: https://github.com/Jordy-20035/ai-reliability-dashboard

**Готов ответить на вопросы.**
