# MVP Demo Script для защиты

**Время демонстрации:** 15-20 минут  
**Цель:** Показать работоспособность всех ключевых компонентов системы

---

## 🎬 Подготовка к демонстрации

### За день до защиты:

1. ✅ Протестировать весь демо-скрипт end-to-end
2. ✅ Убедиться, что все сервисы запускаются
3. ✅ Подготовить резервные скриншоты на случай проблем
4. ✅ Заранее запустить все долгие операции (обучение модели)
5. ✅ Подготовить краткие заметки для каждого шага

### Перед демонстрацией:

- [ ] API запущен: `uvicorn src.api.main:app --reload`
- [ ] Dashboard запущен: `streamlit run src/dashboard/dashboard.py`
- [ ] Модель обучена и сохранена в `models/`
- [ ] Тестовые данные готовы
- [ ] Браузер открыт с нужными вкладками

---

## 📋 Структура демонстрации

### Часть 1: Введение (2 минуты)

**Что говорить:**
> "Добро пожаловать на демонстрацию Trustworthy AI Monitor - системы мониторинга ML моделей, которая отслеживает точность, дрейф данных и справедливость предсказаний в реальном времени."

**Что показать:**
- Главную страницу README (если нужно)
- Структуру проекта

---

### Часть 2: Загрузка и подготовка данных (3 минуты)

**Что говорить:**
> "Система поддерживает несколько датасетов. Мы будем использовать Adult Income dataset для демонстрации."

**Действия:**

1. **Запустить Jupyter notebook или Python скрипт:**

```python
from src.data.load_data import load_adult_data

print("Загрузка Adult Income dataset...")
X_train, X_test, y_train, y_test = load_adult_data()

print(f"✓ Загружено:")
print(f"  - Train: {len(X_train)} образцов")
print(f"  - Test: {len(X_test)} образцов")
print(f"  - Признаков: {X_train.shape[1]}")
print(f"  - Классов: {y_train.nunique()}")
```

2. **Показать структуру данных:**
```python
print("\nПервые несколько строк:")
print(X_train.head())
```

3. **Показать распределение классов:**
```python
print("\nРаспределение классов:")
print(y_train.value_counts())
```

**Что сказать:**
> "Данные загружены и готовы к использованию. Система автоматически определяет типы признаков и применяет соответствующую предобработку."

---

### Часть 3: Обучение модели (4 минуты)

**Вариант A: Через Dashboard (рекомендуется для демо)**

**Действия:**

1. Открыть Dashboard: http://localhost:8501
2. Перейти на страницу "Model Training"
3. Выбрать "XGBoost" как тип модели
4. Нажать "Train Model"
5. Показать результаты обучения

**Что сказать:**
> "Мы обучаем XGBoost модель на тренировочных данных. Система автоматически выполняет предобработку и сохраняет модель."

**Вариант B: Через код (если нужно показать код)**

```python
from src.data.preprocess import DataPreprocessor
from src.models.train_model import ModelTrainer
from src.models.evaluate_model import ModelEvaluator

print("Предобработка данных...")
preprocessor = DataPreprocessor()
X_train_processed = preprocessor.fit_transform(X_train)
X_test_processed = preprocessor.transform(X_test)

print("Обучение XGBoost модели...")
trainer = ModelTrainer(model_type='xgboost')
model = trainer.train(X_train_processed, y_train)

print("Оценка модели...")
evaluator = ModelEvaluator(model)
metrics = evaluator.evaluate(X_test_processed, y_test)

print(f"\n✓ Результаты:")
print(f"  - Accuracy: {metrics['accuracy']:.4f}")
print(f"  - F1 Score: {metrics['f1']:.4f}")
print(f"  - ROC AUC: {metrics['roc_auc']:.4f}")

# Сохранить модель
trainer.save_model('models/demo_model.pkl')
print("\n✓ Модель сохранена")
```

**Показать визуализации:**
- Confusion Matrix
- ROC Curve (если доступно)

**Что сказать:**
> "Модель обучена и показывает хорошие результаты. Теперь мы можем использовать её для предсказаний и мониторинга."

---

### Часть 4: API демонстрация (3 минуты)

**Действия:**

1. **Открыть API документацию:**
   - Открыть http://localhost:8000/docs в браузере
   - Показать Swagger UI

2. **Health Check:**
   ```bash
   # В терминале или через Swagger UI
   GET /manage/health
   ```
   Показать ответ: `{"status": "healthy", "model_loaded": true}`

3. **Модель Info:**
   ```bash
   GET /manage/info
   ```
   Показать информацию о модели

4. **Предсказание:**
   ```bash
   POST /predict/single
   {
     "features": {
       "age": 35,
       "education": "Bachelors",
       "hours-per-week": 40
     }
   }
   ```
   Показать результат предсказания

**Что сказать:**
> "API предоставляет RESTful интерфейс для интеграции с другими системами. Все эндпоинты документированы в Swagger UI."

---

### Часть 5: Детекция дрейфа (4 минуты)

**Действия:**

1. **Сгенерировать данные с дрейфом:**

```python
from src.data.preprocess import create_drift_data
from src.monitoring.drift_metrics import DriftDetector

print("Генерация данных с дрейфом (интенсивность 0.3)...")
X_drifted = create_drift_data(X_test_processed, drift_intensity=0.3)

print("\nДетекция дрейфа...")
detector = DriftDetector(X_test_processed, threshold=0.05)
drift_results = detector.detect_drift(X_drifted, methods=['ks', 'psi'])

print(f"\n✓ Результаты:")
print(f"  - Drift detected: {drift_results['summary']['drift_detected']}")

# Показать PSI результаты
if 'psi' in drift_results:
    print("\nPSI scores (top 5):")
    psi_scores = sorted(drift_results['psi'].items(), 
                       key=lambda x: x[1], reverse=True)[:5]
    for feature, score in psi_scores:
        status = "⚠️ DRIFT" if score > 0.1 else "✓ OK"
        print(f"  {feature}: {score:.4f} {status}")
```

2. **Показать на Dashboard:**
   - Перейти на страницу "Drift Detection"
   - Показать визуализации drift scores

**Что сказать:**
> "Система успешно обнаружила дрейф данных. PSI (Population Stability Index) показывает значительные изменения в распределении признаков. Это важный сигнал, что модель может нуждаться в обновлении."

---

### Часть 6: Мониторинг производительности (3 минуты)

**Действия:**

1. **Показать на Dashboard:**
   - Перейти на страницу "Performance Monitoring"
   - Нажать "Calculate Performance Metrics"
   - Показать все метрики

2. **Или через код:**

```python
from src.monitoring.performance_metrics import PerformanceMonitor

print("Мониторинг производительности...")
monitor = PerformanceMonitor(model)
results = monitor.monitor(X_test_processed, y_test)

print(f"\n✓ Метрики производительности:")
print(f"  - Accuracy: {results['metrics']['accuracy']:.4f}")
print(f"  - F1: {results['metrics']['f1']:.4f}")
print(f"  - Precision: {results['metrics']['precision']:.4f}")
print(f"  - Recall: {results['metrics']['recall']:.4f}")

print(f"\n✓ Latency:")
print(f"  - Mean: {results['latency']['mean_latency_seconds']*1000:.2f}ms")
print(f"  - P95: {results['latency']['p95_latency_seconds']*1000:.2f}ms")
print(f"  - Throughput: {results['latency']['throughput_per_second']:.0f} pred/s")
```

**Что сказать:**
> "Система отслеживает не только метрики точности, но и latency - критично для production систем. Мы видим, что модель обрабатывает X предсказаний в секунду."

---

### Часть 7: Анализ справедливости (4 минуты, если данные поддерживают)

**Важно:** Показывать только если в данных есть чувствительные признаки (gender, race).

**Действия:**

1. **Если данные имеют demographic features:**

```python
from src.monitoring.fairness_metrics import FairnessMonitor

# Проверить наличие demographic features
if 'gender' in X_test.columns or 'sex' in X_test.columns:
    sensitive_feature = 'gender' if 'gender' in X_test.columns else 'sex'
    
    print(f"Анализ справедливости по признаку '{sensitive_feature}'...")
    
    # Предсказания
    y_pred = model.predict(X_test_processed)
    
    # Fairness анализ
    monitor = FairnessMonitor([sensitive_feature], threshold=0.1)
    report = monitor.comprehensive_fairness_report(
        X_test, y_test.values, y_pred
    )
    
    print(f"\n✓ Fairness метрики:")
    if sensitive_feature in report['fairness_metrics']:
        metrics = report['fairness_metrics'][sensitive_feature]
        print(f"  - Demographic Parity Difference: {metrics['demographic_parity_difference']:.4f}")
        print(f"  - Equal Opportunity Difference: {metrics['equal_opportunity_difference']:.4f}")
        print(f"  - Disparate Impact Ratio: {metrics['disparate_impact_ratio']:.4f}")
    
    print(f"\n✓ Нарушения справедливости: {report['total_violations']}")
```

2. **Показать на Dashboard:**
   - Перейти на страницу "Fairness Analysis"
   - Показать групповые метрики

**Если данных нет:**
> "Для полного анализа справедливости нужны demographic признаки. Система поддерживает анализ по любым чувствительным признакам - gender, race, age groups и т.д."

**Что сказать:**
> "Анализ справедливости показывает, насколько равномерно модель предсказывает для разных групп. Это критично для обеспечения этичности AI систем."

---

### Часть 8: Dashboard обзор (2 минуты)

**Действия:**

1. Пройтись по всем страницам Dashboard:
   - Data Explorer
   - Model Training
   - Performance Monitoring
   - Drift Detection
   - Fairness Analysis

2. Показать ключевые визуализации:
   - Графики метрик
   - Confusion matrices
   - Drift scores
   - Group comparisons

**Что сказать:**
> "Dashboard предоставляет интуитивный интерфейс для мониторинга всех аспектов модели. Все метрики обновляются в реальном времени и могут быть экспортированы для отчетов."

---

### Часть 9: Заключение (2 минуты)

**Что сказать:**
> "Мы продемонстрировали ключевые возможности системы:
> 1. ✅ Загрузка и предобработка данных
> 2. ✅ Обучение и оценка моделей
> 3. ✅ API для интеграции
> 4. ✅ Детекция дрейфа данных
> 5. ✅ Мониторинг производительности
> 6. ✅ Анализ справедливости
> 7. ✅ Интерактивный dashboard

> Система готова к использованию в production среде и может быть расширена для дополнительных функций."

**Вопросы?**

---

## 🔧 Troubleshooting во время демо

### Проблема: API не запускается
**Решение:** Показать скриншоты вместо live demo

### Проблема: Долгое обучение модели
**Решение:** Использовать заранее обученную модель

### Проблема: Ошибки в коде
**Решение:** Иметь готовые работающие версии в отдельных файлах

### Проблема: Медленный интернет (если онлайн)
**Решение:** Использовать локальные данные и модели

---

## 📝 Чек-лист перед демо

- [ ] Все сервисы запущены и протестированы
- [ ] Модели обучены и сохранены
- [ ] Тестовые данные готовы
- [ ] Dashboard открыт и работает
- [ ] API документация доступна
- [ ] Резервные скриншоты подготовлены
- [ ] Скрипт прочитан и понят
- [ ] Практика пройдена минимум 2 раза

---

## ⏱️ Тайминг (примерный)

| Часть | Время | Название |
|-------|-------|----------|
| 1 | 2 мин | Введение |
| 2 | 3 мин | Загрузка данных |
| 3 | 4 мин | Обучение модели |
| 4 | 3 мин | API демонстрация |
| 5 | 4 мин | Детекция дрейфа ⭐ |
| 6 | 3 мин | Мониторинг производительности |
| 7 | 4 мин | Анализ справедливости ⭐ |
| 8 | 2 мин | Dashboard обзор |
| 9 | 2 мин | Заключение |
| **Итого** | **~20 мин** | |

**⭐ - ключевые части, на которые обратить особое внимание**

---

**Удачи с демонстрацией! 🎉**

